[tool.ruff]
# Enable rules for Pyflakes (F) and pycodestyle (E)
select = ["E", "F", "W", "I", "N", "B", "UP", "C4", "SIM", "DJ"]
ignore = ["E501"] # Ignore line length violations

# Allow autofix for all enabled rules
fixable = ["ALL"]
unfixable = []

# Exclude a variety of commonly ignored directories.
exclude = [
    ".bzr",
    ".direnv",
    ".eggs",
    ".git",
    ".git-rewrite",
    ".hg",
    ".mypy_cache",
    ".nox",
    ".pants.d",
    ".pytype",
    ".ruff_cache",
    ".svn",
    ".tox",
    ".venv",
    "__pypackages__",
    "_build",
    "buck-out",
    "build",
    "dist",
    "node_modules",
    "venv",
    "migrations",
]
line-length = 88
target-version = "py313"

[tool.ruff.mccobe]
max-complexity = 10
